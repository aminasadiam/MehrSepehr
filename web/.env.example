# Frontend env example for Vite
# Copy to `web/.env` or `web/.env.local` for local development.
# DO NOT put JWT_SECRET here. Frontend should never contain the server secret.

VITE_API_URL=http://localhost:8080/api
